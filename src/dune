(executable
 (name e_uscito_joypad)
 (modules e_uscito_joypad utils views settings joypad_monitor)
 (preprocess
  (pps lwt lwt.unix lwt_ppx))
 (libraries dream batteries malloc timedesc cohttp-lwt-unix lambdasoup re2))

(rule
 (targets views.ml)
 (deps views.eml.ml)
 (action
  (run dream_eml %{deps} --workspace %{workspace_root})))
